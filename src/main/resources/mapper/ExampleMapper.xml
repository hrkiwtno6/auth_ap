<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Nishomapper.ExampleMapper">

    <select id="findByTname" parameterType="int" resultMap="ExampleResultMap">
        SELECT tname FROM tab WHERE tname = #{tname}
    </select>
</mapper>
